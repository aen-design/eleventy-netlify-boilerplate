<section>
  {% for post in postslist | reverse %}
    <article{% if post.url == url %} data-current="current item" {% endif %} class="post">
      <img src="/{{ post.data.cover }}" class="post-img" alt="">
      <a href="{{ post.url }}" class="portfolio-link">
        <div class="post-copy">
          <h3>
            {{ post.data.title }}
          </h3>
          <p>
            {{ post.data.summary }}
          </p>
        </div>
      </a>
    </article>
  {% endfor %}
</section>